# Ticket Booking System Environment Configuration
HOST=0.0.0.0
PORT=8000
DATABASE_URL=sqlite:///./ticket_booking.db
JWT_SECRET=super_secret_jwt_key_ticket_booking_2026
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Seat Hold TTL in minutes
SEAT_HOLD_TTL_MINUTES=10

# Waitlist Offer Time Limit in minutes
WAITLIST_OFFER_TTL_MINUTES=5

# Background Sweeper Interval in seconds
SWEEPER_INTERVAL_SECONDS=5

# SMTP Email Configuration (Leave blank for local mock inbox viewer)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=test@ethereal.email
SMTP_PASS=testpass
SMTP_FROM=tickets@eventmaster.com
